<div class="row">
  <div class="container">
    <h1>
      Welcome, 
      <small>
        <%= current_user.email if current_user %> <%= 'Guest' if !current_user %> 
      </small>
    </h1> 
  </div>
</div>
